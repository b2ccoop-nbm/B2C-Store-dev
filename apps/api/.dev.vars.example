# Copy to .dev.vars (gitignored). See repo root .env.example

DATABASE_URL=postgresql://postgres:postgres@localhost:5434/b2ccoop_store

# Local staff API (Phase 1c — confirm pickup). Never use in production as-is.
DEV_ADMIN_SECRET=b2ccoop-store-dev-admin-change-me
