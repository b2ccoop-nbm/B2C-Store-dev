# Baked into production builds (Astro/Vite production mode).
# Override locally with .env.production.local (gitignored) if needed.

PUBLIC_API_URL=https://b2ccoop-store-api.nmatunog.workers.dev
PUBLIC_STORE_URL=https://store.b2ccoop.com
